{
  "name": "app",
  "version": "1.0.0",
  "browser": "app.js",
  "browserify": {
    "transform": [
      ["babelify", {
        "blacklist": ["regenerator"]
      }],
      ["envify", {
        "NODE_ENV": "production",
        "PORT": "9000"
      }],
      ["reactify", {
        "es6": true
      }]
    ]
  }
}
